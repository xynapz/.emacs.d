#+TITLE: Modules
#+AUTHOR: Angel Dhakal
#+STARTUP: overview
#+OPTIONS: toc:2

* Load Order

Managed in =init.el=:

#+begin_src elisp
(require 'xz-pkg)        ; Packages
(require 'xz-core)       ; Basics
(require 'xz-ui)         ; UI & Theme
(require 'xz-meow)       ; Modal Editing
(require 'xz-editor)     ; Customizations
(require 'xz-completion) ; Completion
(require 'xz-dev)        ; Dev Tools
(require 'xz-org)        ; Org
(require 'xz-export)     ; Export
#+end_src

* xz-core.el

Foundational settings.

| Setting           | Value               |
|-------------------+---------------------|
| Encoding          | UTF-8               |
| Backups           | Disabled            |
| Auto Revert       | Enabled (Global)    |
| Recentf           | Enabled (200 items) |

* xz-pkg.el

Package management infrastructure.

| Package             | Purpose           |
|---------------------+-------------------|
| use-package         | Config macro      |
| which-key           | Key hints         |
| restart-emacs       | Safe restarting   |

* xz-ui.el

Visual appearance.

| Feature         | Implementation       |
|-----------------+----------------------|
| Theme           | xz-nord (Nord fork)  |
| Modeline        | doom-modeline        |
| Fonts           | Iosevka              |
| Icons           | nerd-icons           |

* xz-editor.el

General editor settings.

| Package              | Purpose            |
|----------------------+--------------------|
| display-line-numbers | Relative lines     |
| electric-pair        | Auto brackets      |
| hl-line              | Highlight line     |

* xz-meow.el

Modal editing with Meow.

| Feature          | Description          |
|------------------+----------------------|
| meow             | Core modal editing   |
| QWERTY layout    | Standard bindings    |
| Leader (SPC)     | Quick commands       |
| jk escape        | Fast insert exit     |

See [[file:meow.org][meow.org]] for keybindings.

* xz-completion.el

Modern completion stack.

| Package    | Purpose              |
|------------+----------------------|
| vertico    | Minibuffer UI        |
| orderless  | Fuzzy matching       |
| marginalia | Rich annotations     |
| consult    | Search commands      |
| corfu      | In-buffer completion |
| yasnippet  | Snippets & Templates |

* xz-dev.el

Developer tools and languages.

| Package          | Purpose                  |
|------------------+--------------------------|
| lsp-mode         | Language Server Protocol |
| consult-lsp      | LSP diagnostics search   |
| lsp-treemacs     | Symbols/Errors sidebar   |
| dap-mode         | Debugging (DAP)          |
| apheleia         | Formatting               |
| treesit          | Syntax Highlighting      |
| projectile       | Project management       |
| magit            | Git client               |
| vterm            | Terminal                 |

** Languages Configured
- Python (pylsp + treesit)
- JavaScript/TypeScript (ts-ls + treesit)
- C/C++ (clangd + treesit)
- Go, Rust, YAML, Terraform

* xz-org.el

Org mode setup. See [[file:org-guide.org][org-guide.org]].

* xz-export.el

Blog export functionality.
